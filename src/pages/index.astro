---
import Layout from "@/layouts/Layout.astro";
import Header from "@/components/Header.astro";
import Footer from "@/components/Footer.astro";
import Card from "@/components/Card.astro";
import { getCollection } from "astro:content";
import getSortedPosts from "@/utils/getSortedPosts";
import { SOCIALS } from "@/constants";
import IconRss from "@/assets/icons/IconRss.svg";
import IconArrowRight from "@/assets/icons/IconArrowRight.svg";

const posts = await getCollection("blog", ({ data }) => !data.draft);
const sortedPosts = getSortedPosts(posts);
---

<Layout>
  <Header />
  <main id="main-content" data-layout="index" class="app-layout">
    <!-- Profile Section -->
    <section id="hero" class="pt-16 pb-8">
      <div class="max-w-2xl">
        <h1 class="text-4xl font-bold mb-4 sm:text-5xl">
          Hi, I'm Everfacture
        </h1>
        
        <p class="text-lg text-muted-foreground mb-6 leading-relaxed">
          Building trading bots and tools that solve my problems. Currently in Surabaya, Indonesia. I build what I need and ship it — isn't that the whole point?
        </p>

        <div class="flex flex-wrap gap-3 mb-8">
          <a 
            href="/posts" 
            class="inline-flex items-center gap-2 px-4 py-2 bg-accent text-accent-foreground rounded-md hover:opacity-90 transition-opacity"
          >
            All Posts
            <IconArrowRight class="inline-block w-4 h-4" />
          </a>
          <a 
            href="/about" 
            class="inline-flex items-center gap-2 px-4 py-2 border border-border rounded-md hover:bg-accent/10 transition-colors"
          >
            About
          </a>
        </div>

        <div class="flex items-center gap-4 text-muted-foreground">
          {SOCIALS.map((social) => (
            <a 
              href={social.href}
              target="_blank"
              rel="noopener noreferrer"
              class="hover:text-accent transition-colors"
              title={social.linkTitle}
            >
              <social.icon class="w-5 h-5" />
              <span class="sr-only">{social.linkTitle}</span>
            </a>
          ))}
          <a 
            href="/rss.xml"
            class="hover:text-accent transition-colors"
            title="RSS Feed"
          >
            <IconRss class="w-5 h-5" />
            <span class="sr-only">RSS Feed</span>
          </a>
        </div>
      </div>
    </section>

    <!-- Posts Timeline -->
    <section id="posts" class="pt-4 pb-12">
      <h2 class="text-2xl font-semibold mb-6">Latest Posts</h2>
      <ul class="space-y-6">
        {sortedPosts.map((post) => (
          <Card {...post} variant="h3" />
        ))}
      </ul>
    </section>
  </main>
  <Footer />
</Layout>

<script>
  document.addEventListener("astro:page-load", () => {
    const indexLayout = (document.querySelector("#main-content") as HTMLElement)
      ?.dataset?.layout;
    if (indexLayout) {
      sessionStorage.setItem("backUrl", "/");
    }
  });
</script>
